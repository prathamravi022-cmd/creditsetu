"""URL configuration for notifications app."""
from django.urls import path

urlpatterns = [
    # SMS endpoints are triggered by other apps, not exposed directly.
    # Placeholder for future notification preferences API.
]
